language: node_js
node_js: "8"

cache: npm

script:
- npm audit
- npm run build
- npm run deploy:ci

branches:
  except:
  - "/^v\\d+\\.\\d+\\.\\d+$/" # matches branch names using semantic versioning (e.g. "v0.0.1")
  - "master"